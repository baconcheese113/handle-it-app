mutation CreateNetwork($name: String!) {
  createNetwork(name: $name) {
    __typename
    id
    name
    members {
      __typename
      id
      userId
      role
    }
    createdById
    createdAt
  }
}