targets:
  $default:
    builders:
      graphql_codegen:
        options:
          clients:
            - graphql
            - graphql_flutter
          scalars:
            DateTime:
              type: DateTime
          outputDirectory: __generated__
          generatedFileHeader: "// ignore_for_file: type=lint\n"
